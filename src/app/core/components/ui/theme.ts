import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2962FF",
      light: "#1890FF14",
      dark: "#0039cb",
    },
  },
});

export default theme;
